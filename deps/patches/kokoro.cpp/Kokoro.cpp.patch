diff --git a/Kokoro.cpp b/Kokoro.cpp
index 28ab261..e8fad2e 100644
--- a/Kokoro.cpp
+++ b/Kokoro.cpp
@@ -5,6 +5,7 @@
 #include <regex>
 #include <numeric>
 #include <cstring>
+#include <thread>
 
 // Helper to trim audio (simple amplitude based silence removal)
 std::vector<float> trim_audio(const std::vector<float>& audio, int sample_rate, float threshold_db = 60.0f) {
@@ -14,12 +15,15 @@ std::vector<float> trim_audio(const std::vector<float>& audio, int sample_rate,
     return audio; 
 }
 
-Kokoro::Kokoro(const std::string& model_path, const std::string& voices_path, const std::string& vocab_path) 
+Kokoro::Kokoro(const std::string& model_path, const std::string& voices_path, const std::string& dict_dir, const std::string& vocab_path) 
     : env_(ORT_LOGGING_LEVEL_WARNING, "Kokoro")
 {
     // Initialize session options
     Ort::SessionOptions session_options;
-    session_options.SetIntraOpNumThreads(1);
+    unsigned int threads = std::thread::hardware_concurrency();
+    if (threads == 0) threads = 4;
+    threads = std::min(threads, 4u); // Use up to 4 threads for ONNX runtime
+    session_options.SetIntraOpNumThreads(threads);
     session_options.SetGraphOptimizationLevel(GraphOptimizationLevel::ORT_ENABLE_ALL);
 
     // Load model
@@ -63,7 +67,9 @@ Kokoro::Kokoro(const std::string& model_path, const std::string& voices_path, co
     }
 
     // Initialize Tokenizer
-    tokenizer_ = std::make_unique<Tokenizer>(TokenizerConfig{}, vocab);
+    TokenizerConfig tok_cfg;
+    tok_cfg.dict_dir = dict_dir;
+    tokenizer_ = std::make_unique<Tokenizer>(tok_cfg, vocab);
 }
 
 Kokoro::~Kokoro() {
