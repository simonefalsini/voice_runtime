diff --git a/CMakeLists.txt b/CMakeLists.txt
index 9e322f9..397ff37 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -11,10 +11,7 @@ if (MSVC)
     add_compile_options("/utf-8")
     add_compile_options("/EHsc")
     
-    # 使用静态链接的多线程运行时库
-    set(CMAKE_MSVC_RUNTIME_LIBRARY "MultiThreaded")
-    set(CMAKE_CXX_FLAGS_RELEASE "${CMAKE_CXX_FLAGS_RELEASE} /MT")
-    set(CMAKE_CXX_FLAGS_DEBUG "${CMAKE_CXX_FLAGS_DEBUG} /MTd")
+
     
     # 禁用弃用警告
     add_compile_definitions(_SILENCE_ALL_CXX17_DEPRECATION_WARNINGS)
@@ -33,18 +30,20 @@ if (MSVC)
 endif()
 
 # ONNX Runtime Configuration
-# Priority 1: Check third_party/onnxruntime
-set(LOCAL_ONNX_PATH "${CMAKE_SOURCE_DIR}/third_party/onnxruntime")
-if(EXISTS "${LOCAL_ONNX_PATH}")
-    message(STATUS "Using local ONNX Runtime from: ${LOCAL_ONNX_PATH}")
-    set(ONNXRUNTIME_ROOT "${LOCAL_ONNX_PATH}")
-# Priority 2: User defined ONNXRUNTIME_ROOT
-elseif(NOT DEFINED ONNXRUNTIME_ROOT)
-    # Priority 3: Common system paths
-    if (APPLE)
-        set(ONNXRUNTIME_ROOT "/opt/homebrew/opt/onnxruntime")
+# Priority 1: User defined ONNXRUNTIME_ROOT
+if(NOT DEFINED ONNXRUNTIME_ROOT)
+    # Priority 2: Check third_party/onnxruntime
+    set(LOCAL_ONNX_PATH "${CMAKE_SOURCE_DIR}/third_party/onnxruntime")
+    if(EXISTS "${LOCAL_ONNX_PATH}")
+        message(STATUS "Using local ONNX Runtime from: ${LOCAL_ONNX_PATH}")
+        set(ONNXRUNTIME_ROOT "${LOCAL_ONNX_PATH}")
     else()
-        set(ONNXRUNTIME_ROOT "/usr/local")
+        # Priority 3: Common system paths
+        if (APPLE)
+            set(ONNXRUNTIME_ROOT "/opt/homebrew/opt/onnxruntime")
+        else()
+            set(ONNXRUNTIME_ROOT "/usr/local")
+        endif()
     endif()
 endif()
 
diff --git a/Kokoro.cpp b/Kokoro.cpp
index 28ab261..b93928b 100644
--- a/Kokoro.cpp
+++ b/Kokoro.cpp
@@ -14,7 +14,7 @@ std::vector<float> trim_audio(const std::vector<float>& audio, int sample_rate,
     return audio; 
 }
 
-Kokoro::Kokoro(const std::string& model_path, const std::string& voices_path, const std::string& vocab_path) 
+Kokoro::Kokoro(const std::string& model_path, const std::string& voices_path, const std::string& dict_dir, const std::string& vocab_path) 
     : env_(ORT_LOGGING_LEVEL_WARNING, "Kokoro")
 {
     // Initialize session options
@@ -63,7 +63,9 @@ Kokoro::Kokoro(const std::string& model_path, const std::string& voices_path, co
     }
 
     // Initialize Tokenizer
-    tokenizer_ = std::make_unique<Tokenizer>(TokenizerConfig{}, vocab);
+    TokenizerConfig tok_cfg;
+    tok_cfg.dict_dir = dict_dir;
+    tokenizer_ = std::make_unique<Tokenizer>(tok_cfg, vocab);
 }
 
 Kokoro::~Kokoro() {
diff --git a/Kokoro.h b/Kokoro.h
index 0887c65..4d7d945 100644
--- a/Kokoro.h
+++ b/Kokoro.h
@@ -20,7 +20,7 @@ const int SAMPLE_RATE = 24000;      // Example value
 
 class Kokoro {
 public:
-    Kokoro(const std::string& model_path, const std::string& voices_path, const std::string& vocab_path = "dict/vocab.txt");
+    Kokoro(const std::string& model_path, const std::string& voices_path, const std::string& dict_dir, const std::string& vocab_path);
     ~Kokoro();
 
     
diff --git a/kokoro_main.cpp b/kokoro_main.cpp
index 3a8c209..7474146 100644
--- a/kokoro_main.cpp
+++ b/kokoro_main.cpp
@@ -97,12 +97,13 @@ int main(int argc, char** argv) {
 #endif
     
     std::string vocab_path = "dict/vocab.txt";
+    std::string dict_dir = "dict";
     if (argc > 4) {
         vocab_path = argv[4];
     }
 
     try {
-        Kokoro tts(model_path, voices_path, vocab_path);
+        Kokoro tts(model_path, voices_path, dict_dir, vocab_path);
         
         auto voice = tts.get_voice_style("zf_002"); // Default voice
         auto result = tts.create(text, voice, 1.0f);
diff --git a/missing_symbols.cpp b/missing_symbols.cpp
index d353b18..9845eaf 100644
--- a/missing_symbols.cpp
+++ b/missing_symbols.cpp
@@ -1,3 +1,4 @@
+#if !defined(_MSC_VER)
 #include <cstdint>
 #include <cstring>
 
@@ -27,3 +28,4 @@ extern "C" {
         return size;
     }
 }
+#endif
