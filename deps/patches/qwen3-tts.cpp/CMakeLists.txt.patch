diff --git a/CMakeLists.txt b/CMakeLists.txt
index ea06922..3037c41 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -28,15 +28,41 @@ endif()
 find_package(Threads REQUIRED)
 
 # GGML library paths
-set(GGML_DIR "${CMAKE_CURRENT_SOURCE_DIR}/ggml")
-set(GGML_BUILD_DIR "${GGML_DIR}/build")
+if(NOT DEFINED GGML_DIR)
+    set(GGML_DIR "${CMAKE_CURRENT_SOURCE_DIR}/ggml")
+endif()
+if(NOT DEFINED GGML_BUILD_DIR)
+    set(GGML_BUILD_DIR "${GGML_DIR}/build")
+endif()
 
-# Detect Metal backend availability
-if(APPLE AND EXISTS "${GGML_BUILD_DIR}/src/ggml-metal/libggml-metal.dylib")
-    set(GGML_HAS_METAL ON)
-    message(STATUS "GGML Metal backend found — GPU acceleration enabled")
-else()
-    set(GGML_HAS_METAL OFF)
+# Find GGML libraries robustly
+find_library(GGML_LIB ggml HINTS ${GGML_BUILD_DIR}/src ${GGML_BUILD_DIR}/src/Release ${GGML_BUILD_DIR}/src/Debug)
+find_library(GGML_BASE_LIB ggml-base HINTS ${GGML_BUILD_DIR}/src ${GGML_BUILD_DIR}/src/Release ${GGML_BUILD_DIR}/src/Debug)
+find_library(GGML_CPU_LIB ggml-cpu HINTS ${GGML_BUILD_DIR}/src ${GGML_BUILD_DIR}/src/Release ${GGML_BUILD_DIR}/src/Debug)
+find_library(GGML_CUDA_LIB ggml-cuda HINTS ${GGML_BUILD_DIR}/src/ggml-cuda ${GGML_BUILD_DIR}/src/ggml-cuda/Release ${GGML_BUILD_DIR}/src/ggml-cuda/Debug)
+find_library(GGML_METAL_LIB ggml-metal HINTS ${GGML_BUILD_DIR}/src ${GGML_BUILD_DIR}/src/Release ${GGML_BUILD_DIR}/src/Debug)
+find_library(GGML_VULKAN_LIB ggml-vulkan HINTS ${GGML_BUILD_DIR}/src ${GGML_BUILD_DIR}/src/Release ${GGML_BUILD_DIR}/src/Debug)
+find_library(GGML_BLAS_LIB ggml-blas HINTS ${GGML_BUILD_DIR}/src ${GGML_BUILD_DIR}/src/Release ${GGML_BUILD_DIR}/src/Debug)
+
+set(GGML_LIBS ${GGML_LIB} ${GGML_BASE_LIB} ${GGML_CPU_LIB})
+if(GGML_BLAS_LIB)
+    list(APPEND GGML_LIBS ${GGML_BLAS_LIB})
+endif()
+if(APPLE)
+    list(APPEND GGML_LIBS "-framework Accelerate")
+endif()
+if(GGML_CUDA_LIB)
+    list(APPEND GGML_LIBS ${GGML_CUDA_LIB})
+    find_package(CUDAToolkit REQUIRED)
+    if(CUDAToolkit_FOUND)
+        list(APPEND GGML_LIBS CUDA::cudart CUDA::cublas CUDA::cublasLt CUDA::cuda_driver)
+    endif()
+endif()
+if(GGML_METAL_LIB)
+    list(APPEND GGML_LIBS ${GGML_METAL_LIB})
+    if(APPLE)
+        list(APPEND GGML_LIBS "-framework Metal" "-framework MetalPerformanceShaders" "-framework Foundation" "-framework MetalKit")
+    endif()
 endif()
 
 # Text tokenizer library
@@ -53,14 +79,9 @@ target_link_directories(text_tokenizer PUBLIC
     ${GGML_BUILD_DIR}/src/ggml-metal
 )
 target_link_libraries(text_tokenizer PUBLIC
-    ggml
-    ggml-base
-    ggml-cpu
+    ${GGML_LIBS}
     Threads::Threads
 )
-if(GGML_HAS_METAL)
-    target_link_libraries(text_tokenizer PUBLIC ggml-metal "-framework Metal" "-framework MetalKit")
-endif()
 
 # TTS Transformer library (GGML-based + optional CoreML bridge)
 set(TTS_TRANSFORMER_SOURCES
@@ -86,14 +107,9 @@ target_link_directories(tts_transformer PUBLIC
     ${GGML_BUILD_DIR}/src/ggml-metal
 )
 target_link_libraries(tts_transformer PUBLIC
-    ggml
-    ggml-base
-    ggml-cpu
+    ${GGML_LIBS}
     Threads::Threads
 )
-if(GGML_HAS_METAL)
-    target_link_libraries(tts_transformer PUBLIC ggml-metal "-framework Metal" "-framework MetalKit")
-endif()
 if(APPLE AND QWEN3_TTS_COREML)
     target_link_libraries(tts_transformer PUBLIC
         "-framework Foundation"
@@ -115,14 +131,9 @@ target_link_directories(audio_tokenizer_encoder PUBLIC
     ${GGML_BUILD_DIR}/src/ggml-metal
 )
 target_link_libraries(audio_tokenizer_encoder PUBLIC
-    ggml
-    ggml-base
-    ggml-cpu
+    ${GGML_LIBS}
     Threads::Threads
 )
-if(GGML_HAS_METAL)
-    target_link_libraries(audio_tokenizer_encoder PUBLIC ggml-metal "-framework Metal" "-framework MetalKit")
-endif()
 
 # Audio tokenizer decoder library (GGML-based)
 add_library(audio_tokenizer_decoder STATIC
@@ -138,14 +149,9 @@ target_link_directories(audio_tokenizer_decoder PUBLIC
     ${GGML_BUILD_DIR}/src/ggml-metal
 )
 target_link_libraries(audio_tokenizer_decoder PUBLIC
-    ggml
-    ggml-base
-    ggml-cpu
+    ${GGML_LIBS}
     Threads::Threads
 )
-if(GGML_HAS_METAL)
-    target_link_libraries(audio_tokenizer_decoder PUBLIC ggml-metal "-framework Metal" "-framework MetalKit")
-endif()
 
 # Qwen3 TTS library (full pipeline)
 add_library(qwen3_tts STATIC
