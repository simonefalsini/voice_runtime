diff --git a/qwen3_tts.h b/qwen3_tts.h
index d53f17b..9535381 100644
--- a/qwen3_tts.h
+++ b/qwen3_tts.h
@@ -91,10 +91,20 @@ public:
     // Extract speaker embedding without synthesizing
     bool extract_speaker_embedding(const std::string & reference_audio, std::vector<float> & embedding);
 
-    // Generate speech using a precomputed speaker embedding
+    // Extract speaker embedding from raw audio samples (for caching)
+    bool extract_speaker_embedding(const float * ref_samples, int32_t n_ref_samples,
+                                   std::vector<float> & embedding,
+                                   const tts_params & params = tts_params());
+
+    // Generate speech using a precomputed speaker embedding vector
     tts_result synthesize_with_embedding(const std::string & text,
                                          const std::vector<float> & embedding,
                                          const tts_params & params = tts_params());
+
+    // Generate speech using a precomputed speaker embedding float pointer
+    tts_result synthesize_with_embedding(const std::string & text,
+                                          const float * embedding, int32_t embedding_size,
+                                          const tts_params & params = tts_params());
     
     // Generate speech from text
     // text: input text to synthesize
